
/**
* AUTOGENERATED BY convert.py
* DO NOT EDIT THIS FILE!
*
* IF MODIFICATIONS NEED TO BE MADE, MODIFY can_data.txt!
*
* CAN Defintiion for ECU 'FSCM'
*/

#ifndef __ECU_FSCM_H_
#define __ECU_FSCM_H_

#include <stdint.h>
    
#define FSCM_STAT_EGS53_CAN_ID 0x02E5
#define NM_FSCM_EGS53_CAN_ID 0x041F

/** Fuel Pressure Sensor State / Status Fuel pressure sensor */
enum class FSCM_STAT_FuelPressSens_Stat_V2_EGS53 : uint16_t {
	OK = 0, // Normal Operation Mode
	SHRT_GND = 1, // Invalid Fuel Pressure Sensor Signal, Shortcut to Ground
	SHRT_BAT = 2, // Invalid Fuel Pressure Sensor Signal, Shortcut to Battery Voltage
	IMPLSBL = 3, // Implausible Fuel Pressure Sensor Signal
	PWR_FLT = 4, // Fuel Pressure Sensor Power Supply Fault
	SNA = 7, // Signal Not Available
};

/** Fuel Pump State / Status Fuel Pump */
enum class FSCM_STAT_FuelPmp_Stat_V2_EGS53 : uint16_t {
	OK = 0, // Normal Operation Mode
	SHRT = 1, // Fuel Pump Shortcut
	OPN = 2, // Fuel Pump Open Circuit
	LHOM = 3, // LIMP-Home Operation Fashion
};

/** Fuel System Control Module State / Status FSCM */
enum class FSCM_STAT_FSCM_Stat_AMG_EGS53 : uint16_t {
	OK = 0, // normal surgery
	WARN_TEMP = 1, // Temperature High Warning
	ALM_TEMP = 2, // Temperature High Alarm
	START_FAIL = 3, // Fuel Pump Start Up Failed
	FLT_GNRL = 4, // General Fault
	NDEF5 = 5, // Not Defined
	NDEF6 = 6, // Not Defined
	SNA = 7, // Signal Not Available
};

/** Network Management Mode / Network Management Mode */
enum class NM_FSCM_NM_Mode_EGS53 : uint16_t {
	LHOM = 252, // LIMP-HOME Fashion
	RING = 253, // ring fashion
	ALIVE = 254, // Alive mode
	SNA = 255, // Signal Not Available
};

/** Network Management UserData Launch Type / Network Management UserData Sendart */
enum class NM_FSCM_NM_Ud_Launch_EGS53 : uint16_t {
	BROADCAST = 4, // Broadcast or Start Alive
	SNA = 63, // Signal Not Available
};

/** Network Management UserData Service No./netzmanagement UserData service */
enum class NM_FSCM_NM_Ud_Srv_EGS53 : uint16_t {
	DATA_OK_BC = 1, // UserData Transmission OK (Broadcast)
	WAKEUP_SA = 2, // Wakeup status (start alive)
	SBC_STAT_BC = 5, // System Base Chip Status (Broadcast)
	AWAKE_BC = 15, // Stay Awake Reason (Broadcast)
	SNA = 255, // Signal Not Available
};

/** Wakeup Reason / Wake-up */
enum class NM_FSCM_WakeupRsn_FSCM_EGS53 : uint16_t {
	NETWORK = 0, // Wakeup by Network
	TERM_15 = 130, // Wakeup by Discrete Terminal 15 Signal
	SNA = 255, // Signal Not Available
};

/** Network Identification No./netzwerk-id */
enum class NM_FSCM_Nw_Id_EGS53 : uint16_t {
	BACKBONE = 4, // Backbone CAN
	DIAGNOSTICS = 5, // Diagnostics CAN
	BODY = 6, // Body CAN
	CHASSIS = 7, // Chassis CAN
	POWERTRAIN = 8, // Powertrain Can
	PT_SENSOR = 9, // Powertrain Sensor CAN
	DYNAMICS = 11, // Dynamics CAN
	HEADUNIT = 14, // HeadUnit CAN
	IMPACT = 15, // Impact CAN
	MULTIPURPOSE = 16, // Multipurpose CAN
	SNA = 255, // Signal Not Available
};



typedef union {
	uint64_t raw;
	uint8_t bytes[8];
	struct {
		/** Evaporative System Integrity Monitor Contact Closing Time / Conclusion of the Contact Integrity Monitoring Evaporation System **/
		uint16_t ESIM_Cntct_ClsTm: 10;
		 /** BITFIELD PADDING. DO NOT CHANGE **/
		uint8_t __PADDING1__: 4;
		/** Evaporative System Integrity Monitor Contact State / Contact State Integrity Monitoring Evaporation System **/
		bool ESIM_Cntct_Stat: 1;
		/** Evaporative System Integrity Monitor Contact State at Engine Shut-Off / Contact Rate Integrity Monitoring Evaporation System for Engine Stop **/
		bool ESIM_Cntct_Stat_EngShutOff: 1;
		/** Fuel System Control Module State / Status FSCM **/
		FSCM_STAT_FSCM_Stat_AMG_EGS53 FSCM_Stat_AMG: 3;
		 /** BITFIELD PADDING. DO NOT CHANGE **/
		uint8_t __PADDING2__: 5;
		/** Actual Fuel Pump # 1 Input Duty Cycle / News Input Dewy Rating Fuel Pump 1 **/
		uint8_t FuelPmp1_InDtyCyc: 8;
		 /** BITFIELD PADDING. DO NOT CHANGE **/
		uint8_t __PADDING3__: 8;
		/** Fuel Pump State / Status Fuel Pump **/
		FSCM_STAT_FuelPmp_Stat_V2_EGS53 FuelPmp_Stat_V2: 2;
		/** Fuel Pressure Sensor State / Status Fuel pressure sensor **/
		FSCM_STAT_FuelPressSens_Stat_V2_EGS53 FuelPressSens_Stat_V2: 3;
		 /** BITFIELD PADDING. DO NOT CHANGE **/
		bool __PADDING4__: 1;
		/** Fuel Pressure: InCreased Hydraulic Resistance / Fuel pressure: Increased hydraulic resistance **/
		bool FuelPress_IncHydrRst: 1;
		/** Fuel Pressure Request Not available / requirement Fuel pressure not available **/
		bool FuelPress_Rq_NA: 1;
		/** Actual Fuel Pump Duty Cycle / Current duty cycle fuel pump **/
		uint8_t FuelPmpDtyCyc: 8;
		/** Actual Fuel Pressure / Current fuel pressure **/
		uint8_t FuelPress: 8;
	} __attribute__((packed));
	/** Gets CAN ID of FSCM_STAT_EGS53 **/
	uint32_t get_canid(){ return FSCM_STAT_EGS53_CAN_ID; }
} FSCM_STAT_EGS53;



typedef union {
	uint64_t raw;
	uint8_t bytes[8];
	struct {
		/** Network Identification No./netzwerk-id **/
		NM_FSCM_Nw_Id_EGS53 Nw_Id: 8;
		 /** BITFIELD PADDING. DO NOT CHANGE **/
		uint8_t __PADDING1__: 8;
		/** Counter for Module Wakeup States During Network Sleep / Counter for ECUs Internal Wachzustäustände during bus rest **/
		uint8_t WakeupCnt: 8;
		/** Wakeup Reason / Wake-up **/
		NM_FSCM_WakeupRsn_FSCM_EGS53 WakeupRsn_FSCM: 8;
		/** Network Management UserData Service No./netzmanagement UserData service **/
		NM_FSCM_NM_Ud_Srv_EGS53 NM_Ud_Srv: 8;
		/** Network Management UserData Launch Type / Network Management UserData Sendart **/
		NM_FSCM_NM_Ud_Launch_EGS53 NM_Ud_Launch: 6;
		/** Network Management Sleep Acknowledge / Network Management Sleep Acknowledge **/
		bool NM_Sleep_Ack: 1;
		/** Network Management Sleep Indication / Network Management Sleep Indication **/
		bool NM_Sleep_Ind: 1;
		/** Network Management Logical Successor / Network Management Logical Successor **/
		uint8_t NM_Successor: 8;
		/** Network Management Mode / Network Management Mode **/
		NM_FSCM_NM_Mode_EGS53 NM_Mode: 8;
	} __attribute__((packed));
	/** Gets CAN ID of NM_FSCM_EGS53 **/
	uint32_t get_canid(){ return NM_FSCM_EGS53_CAN_ID; }
} NM_FSCM_EGS53;



class ECU_FSCM {
	public:
        /**
         * @brief Imports the CAN frame given the CAN ID, CAN Contents, and current timestamp
         *
         * Returns true if the frame was imported successfully, and false if import failed (Due to non-matching CAN ID).
         *
         * NOTE: The endianness of the value cannot be guaranteed. It is up to the caller to correct the byte order!
         */
        bool import_frames(uint64_t value, uint32_t can_id, uint32_t timestamp_now) {
            uint8_t idx = 0;
            bool add = true;
            switch(can_id) {
                case FSCM_STAT_EGS53_CAN_ID:
                    idx = 0;
                    break;
                case NM_FSCM_EGS53_CAN_ID:
                    idx = 1;
                    break;
                default:
                    add = false;
                    break;
            }
            if (add) {
                LAST_FRAME_TIMES[idx] = timestamp_now;
                FRAME_DATA[idx] = value;
            }
            return add;
        }
        
        /** Sets data in pointer to FSCM_STAT
          * 
          * If this function returns false, then the CAN Frame is invalid or has not been seen
          * on the CANBUS network yet. Meaning it's data cannot be used.
          *
          * If the function returns true, then the pointer to 'dest' has been updated with the new CAN data
          */
        bool get_FSCM_STAT(const uint32_t now, const uint32_t max_expire_time, FSCM_STAT_EGS53* dest) const {
            bool ret = false;
            if (dest != nullptr && LAST_FRAME_TIMES[0] <= now && now - LAST_FRAME_TIMES[0] < max_expire_time) {
                dest->raw = FRAME_DATA[0];
                ret = true;
            }
            return ret;
        }
            
        /** Sets data in pointer to NM_FSCM
          * 
          * If this function returns false, then the CAN Frame is invalid or has not been seen
          * on the CANBUS network yet. Meaning it's data cannot be used.
          *
          * If the function returns true, then the pointer to 'dest' has been updated with the new CAN data
          */
        bool get_NM_FSCM(const uint32_t now, const uint32_t max_expire_time, NM_FSCM_EGS53* dest) const {
            bool ret = false;
            if (dest != nullptr && LAST_FRAME_TIMES[1] <= now && now - LAST_FRAME_TIMES[1] < max_expire_time) {
                dest->raw = FRAME_DATA[1];
                ret = true;
            }
            return ret;
        }
            
	private:
		uint64_t FRAME_DATA[2];
		uint32_t LAST_FRAME_TIMES[2];
};
#endif // __ECU_FSCM_H_